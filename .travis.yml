language: cpp
dist: trusty
sudo: required
compiler:
  - gcc
  - clang
before_script:
 - sudo apt-get install libboost-dev
script:
 - autoreconf -i 
 - ./configure
 - ./testrun.sh
notifications:
  email:
    - bert.hubert@powerdns.com

