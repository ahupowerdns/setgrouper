language: cpp
dist: trusty
sudo: false
compiler:
  - gcc
  - clang
before_script:
 - echo hoi
script:
 - autoreconf -i 
 - ./configure
 - ./testrun.sh
notifications:
  email:
    - bert.hubert@powerdns.com

